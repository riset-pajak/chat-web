# 2026-04-07

## Activities
- Inisialisasi project chat-web sebagai FastAPI service (API + Web Chat, satu service)
- Setup pyproject.toml, src/chatweb/, CLI entry point (`chat-web` command)
- Setup endpoints dasar: `/health`, `/` (discovery), `/chat` (placeholder template)
- Setup struktur: routes/, templates/, static/, tests/
- Dokumentasi: README.md, AGENTS.md, SOUL.md, TASKS.md, MEMORY.md, TOOLS.md
- Architecture decision: single FastAPI service yang serve REST API (`/api/*`) untuk consumer (telegram-bot + browser) dan Web Chat (`/chat`)

## Decisions
- **Single service**: API dan Web Chat dalam satu FastAPI app (bukan terpisah)
- **Consumer pattern**: telegram-bot consume API chat-web, bukan langsung query corpus
- **LLM integration**: API akan handle retrieval + LLM (explain, compare, ask)
- **Data source**: `../corpus-preparation/data/output/corpus.jsonl` -> SQLite dengan FTS5

## Phase Status
- ✅ Phase 1: FastAPI app init, health + discovery + chat placeholder, docs lengkap
- Next: Phase 2a (SQLite ingestion), 2b (API endpoints), 2c (LLM integration), 2d (Web Chat UI), 2e (bot integration)

## Commits Sesi Ini
| Commit | Pesan |
|--------|-------|
| `1bc08ed` | feat: init FastAPI service (API + Web Chat dalam satu service) |
| `c5df786` | docs: add TASKS.md, MEMORY.md, TOOLS.md |
